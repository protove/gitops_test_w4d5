apiVersion: v1
kind: Service
metadata:
  name: gitops-web
  namespace: week4-gitops
  labels:
    app: gitops-web
spec:
  selector:
    app: gitops-web
  ports:
    - name: http
      port: 80
      targetPort: http
